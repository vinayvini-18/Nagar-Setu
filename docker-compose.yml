version: '3.8'

services:
  nagarsetu:
    build: .
    container_name: nagarsetu-flask
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: production
      PORT: 5000
    restart: unless-stopped
